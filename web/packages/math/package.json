{
  "name": "@xs/math",
  "version": "0.0.0",
  "description": "Pure 2D math primitives — points, vectors, angles, curves, geometry — ported from ExcalidrawMath.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
