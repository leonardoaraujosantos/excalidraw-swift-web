{
  "name": "@xs/geometry",
  "version": "0.0.0",
  "description": "Element-aware geometry — bounds, hit-testing, binding, snapping, culling — ported from ExcalidrawGeometry.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@xs/math": "workspace:*",
    "@xs/model": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
